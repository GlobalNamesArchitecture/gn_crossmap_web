machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install -U docker-compose
database:
  override:
    - echo "nothing needed"
test:
  override:
    - docker-compose up
    - docker-compose run web rake
